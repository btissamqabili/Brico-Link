<x-app-layout>
    <div class="page-frame"><div class="flex flex-col justify-between gap-5 border-b border-[#e5dfd4] pb-8 sm:flex-row sm:items-end"><div><p class="eyebrow">Votre atelier</p><h1 class="display-title mt-3">Mes services</h1><p class="mt-3 text-sm leading-6 text-[#6F6862]">Présentez clairement ce que vous savez faire, pour que les bonnes demandes vous trouvent.</p></div><a href="{{ route('services.create') }}" class="action-primary">Ajouter un service <span class="ml-3">+</span></a></div>@if(session('success'))<div class="notice-success mt-6">{{ session('success') }}</div>@endif<div class="mt-8 grid gap-5 md:grid-cols-2">@forelse($services as $service)<article class="surface flex flex-col justify-between p-6 sm:p-7"><div><div class="flex items-start justify-between gap-4"><p class="eyebrow">{{ $service->categorie->nom ?? 'Savoir-faire' }}</p><span class="font-serif text-xl text-[#869B7E]">✦</span></div><h2 class="mt-4 font-serif text-2xl">{{ $service->nom }}</h2><p class="mt-3 text-sm leading-6 text-[#6F6862]">{{ $service->description }}</p></div><div class="mt-8 flex flex-col gap-4 border-t border-[#e5dfd4] pt-5 sm:flex-row sm:items-center sm:justify-between"><p class="font-serif text-xl">{{ $service->prix ? number_format($service->prix, 2, ',', ' ') . ' DH' : 'Sur devis' }}</p><div class="flex items-center gap-3"><a href="{{ route('services.edit', $service) }}" class="action-quiet">Modifier</a><form method="POST" action="{{ route('services.destroy', $service) }}">@csrf @method('DELETE')<button type="submit" onclick="return confirm('Voulez-vous vraiment supprimer ce service ?')" class="min-h-11 px-2 text-sm font-semibold text-[#7F2020]">Supprimer</button></form></div></div></article>@empty<div class="surface p-10 text-center md:col-span-2"><p class="font-serif text-2xl">Votre atelier est encore silencieux.</p><p class="mt-2 text-sm text-[#6F6862]">Ajoutez votre premier service pour être visible auprès des clients.</p><a href="{{ route('services.create') }}" class="action-primary mt-6">Présenter un service</a></div>@endforelse</div></div>
</x-app-layout>
