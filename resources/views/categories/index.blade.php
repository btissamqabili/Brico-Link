<x-app-layout>
    <div class="page-frame"><div class="flex flex-col justify-between gap-5 border-b border-[#e5dfd4] pb-8 sm:flex-row sm:items-end"><div><p class="eyebrow">Administration · structure</p><h1 class="display-title mt-3">Catégories</h1><p class="mt-3 text-sm text-[#6F6862]">Les familles de savoir-faire proposées sur la plateforme.</p></div><a href="{{ route('categories.create') }}" class="action-primary">Ajouter une catégorie <span class="ml-3">+</span></a></div>@if(session('success'))<div class="notice-success mt-6">{{ session('success') }}</div>@endif<div class="surface mt-8 overflow-hidden">@forelse($categories as $categorie)<div class="list-row flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-[0.14em] text-[#869B7E]">Catégorie #{{ $categorie->id }}</p><h2 class="mt-2 font-serif text-2xl">{{ $categorie->nom }}</h2><p class="mt-1 text-sm text-[#6F6862]">{{ $categorie->description ?? 'Aucune description' }}</p></div><div class="flex items-center gap-4 text-sm"><a href="{{ route('categories.edit', ['category' => $categorie->id]) }}" class="font-semibold text-[#7F2020]">Modifier</a><form action="{{ route('categories.destroy', ['category' => $categorie->id]) }}" method="POST">@csrf @method('DELETE')<button type="submit" onclick="return confirm('Voulez-vous vraiment supprimer cette catégorie ?')" class="font-semibold text-[#7F2020]">Supprimer</button></form></div></div>@empty<div class="p-10 text-center text-[#6F6862]">Aucune catégorie disponible.</div>@endforelse</div></div>
</x-app-layout>
