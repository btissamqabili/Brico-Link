<x-app-layout>
    <div class="page-frame"><div class="mx-auto max-w-3xl"><div class="border-b border-[#e5dfd4] pb-8"><p class="eyebrow">Votre proposition</p><h1 class="display-title mt-3">Modifier mon offre</h1><p class="mt-3 text-sm leading-6 text-[#6F6862]">{{ $offre->mission->titre }}</p></div><div class="surface mt-8 p-6 sm:p-9">@if($errors->any())<div class="notice-error mb-7"><ul class="list-disc space-y-1 pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form method="POST" action="{{ route('offres.update', $offre) }}" class="grid gap-6">@csrf @method('PUT')<div><label for="prix_propose" class="field-label">Prix proposé (DH)</label><input type="number" name="prix_propose" id="prix_propose" min="0" step="0.01" required value="{{ old('prix_propose', $offre->prix_propose) }}" class="field-control">@error('prix_propose')<p class="mt-1 text-sm text-[#7F2020]">{{ $message }}</p>@enderror</div><div><label for="delai_execution" class="field-label">Délai d’exécution (jours)</label><input type="number" name="delai_execution" id="delai_execution" min="1" max="365" required value="{{ old('delai_execution', $offre->delai_execution) }}" class="field-control">@error('delai_execution')<p class="mt-1 text-sm text-[#7F2020]">{{ $message }}</p>@enderror</div><div><label for="message" class="field-label">Votre message au client</label><textarea name="message" id="message" rows="5" required class="field-control">{{ old('message', $offre->message) }}</textarea>@error('message')<p class="mt-1 text-sm text-[#7F2020]">{{ $message }}</p>@enderror</div><div class="flex flex-col gap-3 border-t border-[#e5dfd4] pt-6 sm:flex-row"><button type="submit" class="action-primary">Enregistrer l’offre</button><a href="{{ route('prestataire.missions.show', $offre->mission) }}" class="action-quiet">Annuler</a></div></form></div></div></div>
</x-app-layout>
